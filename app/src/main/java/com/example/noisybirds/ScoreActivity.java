package com.example.noisybirds;import androidx.appcompat.app.AppCompatActivity;import android.os.Bundle;import android.util.Log;import android.widget.Adapter;import android.widget.ArrayAdapter;import android.widget.ListView;import java.util.ArrayList;import java.util.List;import io.paperdb.Paper;public class ScoreActivity extends AppCompatActivity {    private ListView lstScore;    private List<Integer> scores;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate( savedInstanceState );        setContentView( R.layout.activity_score );        Paper.init( this );        lstScore = findViewById( R.id.lstScore );        scores = new ArrayList<>();        //scores = Paper.book().read( "scores" );//        for (int i = 0; i < scores.size(); i++) {//            Log.d( "mm", scores.get( i ) + "" );//        }        ArrayAdapter adapter = new ArrayAdapter( ScoreActivity.this,                android.R.layout.simple_list_item_1, scores );        //lstScore.setAdapter( adapter );    }}